package com.exchangeBE.exchange.entity.Trip;

import jakarta.persistence.*;
import lombok.Getter;
import lombok.Setter;
import lombok.ToString;

import java.time.LocalDateTime;
/*
@Getter
@Setter
@ToString
@Entity
@Table(name = "user")
public class UserEntity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    private Long id;

    @Column(name = "email", nullable = false)
    private String email;

    @Column(name = "nickname", nullable = false)
    private String nickname;

    @Column(name = "password", nullable = false)
    private String password;

    @Column(name = "domestic_university")
    private String domesticUniversity;

    @Column(name = "domestic_major")
    private String domesticMajor;

    @Column(name = "exchange_university")
    private String exchangeUniversity;

    @Column(name = "exchange_money", nullable = false)
    private Integer exchangeMoney = 0;

    @Column(name = "exchange_period_start", nullable = false)
    private LocalDateTime exchangePeriodStart;

    @Column(name = "exchange_period_end", nullable = false)
    private LocalDateTime exchangePeriodEnd;

    @Column(name = "enrollment_certificate")
    private String enrollmentCertificate;

    @Column(name = "approve", nullable = false)
    private Boolean approve = false;

    @Column(name = "membership_level", nullable = false)
    @Enumerated(EnumType.STRING)
    private MembershipLevel membershipLevel = MembershipLevel.FRESHMAN;

    @Column(name = "total_view", nullable = false)
    private Integer totalView = 0;

    @Column(name = "total_scrap", nullable = false)
    private Integer totalScrap = 0;

    public enum MembershipLevel {
        FRESHMAN, SOPHOMORE, JUNIOR
    }
}*/
